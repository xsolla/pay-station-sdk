export enum PriceTextAttributes {
  priceLineContent = 'price-line-content',
  priceLineAmount = 'price-line-amount',
  priceLineCurrency = 'price-line-currency',
  amount = 'amount',
  currency = 'currency',
}

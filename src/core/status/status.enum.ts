export enum StatusEnum {
  created = 'created',
  awaiting = 'awaiting',
  processing = 'processing',
  authorized = 'authorized',
  held = 'held',
  done = 'done',
  error = 'error',
  canceled = 'canceled',
}
